<?php

use Illuminate\Database\Seeder;
use App\Models\Business;

class BussinessSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        
        // $business = config('constants.partner');
        // $languages = config('laravellocalization.supportedLocales');

        // foreach($business as $key => $bs){
        //     foreach($languages as $key => $language){
        //         $input[$key]['name'] = $bs;
        //     }
        //     Business::create($input);
        // }

    }
}
